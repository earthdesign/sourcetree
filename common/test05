test04
git config --global user.email "earth.designhi@gmail.com"
git config --global user.name "earthdesign"

git config --global user.name "earthdesign"
git config --global user.name "earthdesign"
git config --global user.name "earthdesign"
